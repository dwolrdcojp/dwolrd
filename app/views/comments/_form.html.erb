<%= bootstrap_form_for([@item, @item.comments.build]) do |f| %>
    <%= f.text_area :body, placeholder: "I want this...", hide_label: true %>
    <center><%= f.submit "Ask a question" %></center>
<% end %>
